// var, let, const

// number, string, boolean, null, undefined | typeof null = object

// let variableName = value

// String methods - a.length, a.toUPperCase(), a.toLowerCase(), substr(start, end), split('basis)

// Arrays - Collection of diffrenet data types 
// Methods - push(), pop(), unshift(), Array.isArray(array), indexOf('oranges')

// Objects - { key: value } | . operator

// Arrayof Obects - [ {Object 1}, {Object 2}, {Object 3}, ... ]

// LOOPS

// Conditionals - if, else, else-if, switch

// Voter System | if male - 21 years | female - 18 years

// Switch

// FUNCTIONS

// function to add two numbers

function greeting2(name) {
    return `Goodbye ${name}`;
}

// function greeting(name) {
//     ans = greeting2(name);
//     return ans;
// }

// Calling the function
// console.log(greeting('Sai'));


// ES6 Arrrow Functions -> Short cut of writing functions

// const variableName/ functionName = (parameter(s)) => { // Body // }

// const add = (a, b) => {
//     // Body of the function
//     console.log(`The sum is ${a+b}`);
// }

// add(100, 200)

// const greeting = (name) => {
//     ans = greeting2(name);
//     console.log(ans);
// }
// console.log(greeting('Sai'));